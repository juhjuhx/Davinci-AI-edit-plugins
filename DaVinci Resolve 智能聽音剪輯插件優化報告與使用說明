# DaVinci Resolve 智能聽音剪輯插件優化報告與使用說明

**作者：** Manus AI

**日期：** 2026年6月1日

## 摘要

本報告針對用戶提供的 `davinci_smart_aroll_local.py` DaVinci Resolve 插件進行了深入分析，旨在評估其可用性、易用性，並提出一套全面的優化方案。優化目標包括：實現更輕量、易於啟動的架構，提升性能，並充分利用 CUDA、Vulkan、OpenCL、SYCL 等多種 GPU 加速管道。報告中設計了一個基於 PySide6 的高效圖形用戶介面 (GUI)，並提供了一份優化後的插件代碼 (`davinci_smart_aroll_v2.py`) 及詳細的使用說明。

## 1. 原始插件 (`davinci_smart_aroll_local.py`) 分析

用戶提供的原始腳本展示了將語音識別 (Whisper) 和大語言模型 (LLM) 整合到 DaVinci Resolve 剪輯工作流中的創新思路。其核心優勢和待改進點如下：

### 1.1 優勢

*   **完全本地化處理**：採用 `faster-whisper` 進行語音轉錄，`llama-cpp-python` 載入 GGUF 模型進行語義分析，無需外部 API 依賴，確保數據隱私與處理速度。這對於離線工作或對數據安全有嚴格要求的用戶至關重要。
*   **初步的 GPU 加速意識**：內建 `detect_backend` 函數，能識別 Apple Silicon (Metal) 和 NVIDIA CUDA 環境，並配置 `llama-cpp-python` 使用 GPU 層 (`LLM_N_GPU_LAYERS = -1`)，這為後續的 GPU 優化奠定了基礎。
*   **模組化設計**：將 Whisper 模型、LLM 模型與 DaVinci Resolve 整合邏輯分離為獨立的類別 (`LocalWhisper`, `LocalLLM`, `ResolveIntegration`)，有助於代碼組織與維護，便於未來的功能擴展。
*   **綜合分析流程**：結合了語音轉錄、停頓偵測、信心度過濾、LLM 語義分析及基於正則表達式的暗號偵測，提供多維度的剪輯建議，顯著提升了口播剪輯的效率。
*   **DaVinci Resolve 整合**：能將分析結果直接應用於 DaVinci Resolve 時間軸，透過 Clip 顏色標記與 Marker 備註，直觀地呈現剪輯建議，與剪輯師的工作習慣高度契合。
*   **參數可配置性**：腳本頂部提供清晰的用戶配置區，方便調整模型大小、語言、LLM 路徑等關鍵參數，增加了靈活性。

### 1.2 待改進點

*   **用戶介面 (UI/UX)**：原始腳本為純命令行操作，缺乏直觀的圖形介面。這導致操作不便，不符合 DaVinci Resolve 剪輯師的視覺化工作流程，嚴重影響了易用性。
*   **GPU 後端支援的廣度與彈性**：
    *   `faster-whisper` 對 Vulkan/OpenCL/SYCL 的顯式支援不足，主要依賴 CUDA 或底層自動調用 (如 Metal)。
    *   `llama-cpp-python` 的 Vulkan/SYCL 支援依賴於特定編譯版本的安裝，腳本中缺乏對這些後端的自動檢測或更友好的配置引導，增加了用戶的配置難度。
    *   `detect_backend` 函數的邏輯可以更精細，以區分不同 GPU 類型並提供最佳配置，提升自動化程度。
*   **性能瓶頸與資源管理**：
    *   **音訊提取**：每次分析影片都需要將音訊提取為 WAV 文件，可能產生 I/O 開銷。對於大型影片，這是一個潛在的瓶頸，影響處理速度。
    *   **模型載入**：雖然模型只載入一次，但在首次啟動時仍需等待。若能將模型載入與 UI 啟動分離，可提升用戶體驗，避免 UI 假死。
    *   **LLM 批次處理**：目前的批次處理策略是固定的 `LLM_BATCH_SIZE`，未考慮到不同模型或硬體配置下的最佳批次大小，可能無法充分利用硬體資源。
*   **錯誤處理與用戶反饋**：目前的錯誤處理相對基礎，當模型載入失敗或推理出錯時，用戶可能難以理解問題根源，影響問題排查效率。
*   **可擴展性**：若要引入更多分析模型或功能，現有腳本的結構可能需要進一步抽象化，以支持更複雜的插件生態。

## 2. 優化架構方案

為了解決上述問題並提升插件的整體表現，本報告提出一個分層架構，將 UI、業務邏輯和模型管理解耦，以實現更好的可用性、性能和可擴展性。

```mermaid
graph TD
    A[DaVinci Resolve UI] --> B(Plugin Frontend: PySide6 Application)
    B -- IPC / Local API --> C(Plugin Backend: Python API Server)
    C --> D{Model Manager}
    D --> D1[Whisper Model (faster-whisper / whisper.cpp)]
    D --> D2[LLM Model (llama-cpp-python)]
    D --> D3[GPU Backend Abstraction (CUDA/Vulkan/OpenCL/SYCL)]
    C --> E[Analysis Engine]
    E --> E1[Audio Extraction (FFmpeg)]
    E --> E2[Transcription Logic]
    E --> E3[Semantic Analysis Logic]
    E --> E4[Rule-based Detection]
    C --> F[DaVinci Integration Layer]
    F --> G[DaVinci Resolve Scripting API]
```

### 2.1 架構組件說明

1.  **Plugin Frontend (PySide6 Application)**：
    *   **功能**：這是用戶直接互動的圖形介面。考慮到 DaVinci Resolve Workflow Integration 插件的限制 [6]，一個獨立的 PySide6 應用程式作為插件的 UI 是一個更可行且功能豐富的選擇。此介面將負責接收用戶輸入、顯示進度、展示分析結果。
    *   **優勢**：提供直觀、響應迅速的用戶體驗，避免 DaVinci Resolve 主程序因耗時操作而卡頓。

2.  **Plugin Backend (Python API Server)**：
    *   **功能**：一個輕量級的 Python API 服務（例如使用 Flask 或 FastAPI）在後台運行，負責處理來自前端的請求，協調模型推理與分析邏輯。前端與後端之間通過 IPC (Inter-Process Communication) 或本地 HTTP API 進行通信。
    *   **優勢**：將耗時的 AI 處理與 UI 執行緒分離，避免 UI 卡頓，並允許未來更靈活的部署方式（例如作為獨立服務）。

3.  **Model Manager (模型管理器)**：
    *   **功能**：統一管理 `faster-whisper` (或 `whisper.cpp`) 和 `llama-cpp-python` 模型的生命週期，包括首次載入、緩存、卸載。
    *   **GPU 後端抽象層**：提供一個統一的介面來配置和選擇不同的 GPU 後端（CUDA, Vulkan, OpenCL, SYCL）。這將通過動態載入或條件編譯 `llama-cpp-python` 和 `faster-whisper` 的特定版本來實現，並提供用戶友好的配置選項。
    *   **硬體自動偵測增強**：擴展 `detect_backend` 函數，不僅識別硬體類型，還能推薦或自動配置最佳的 `compute_type` 和 `n_gpu_layers` 參數。

4.  **Analysis Engine (分析引擎)**：
    *   **功能**：包含現有腳本中的 `transcribe`、`detect_pauses`、`filter_by_confidence`、`analyze_with_llm`、`apply_cue_rules` 等核心邏輯。
    *   **音訊提取模組**：優化 `ffmpeg` 調用，考慮直接從影片流中讀取音訊數據，減少臨時文件 I/O。或者，提供選項讓用戶預先提取音訊。
    *   **結果整合與優化**：將各階段的分析結果更有效地整合到 `Segment` 對象中，並提供更精細的標記與建議。

5.  **DaVinci Integration Layer (DaVinci 整合層)**：
    *   **功能**：負責與 DaVinci Resolve Scripting API 進行交互，將分析引擎的結果轉換為 DaVinci Resolve 可識別的 Clip 顏色、Marker 和其他元數據。
    *   **優勢**：提供更豐富的整合選項，例如創建新的時間軸、自動調整 Clip 長度等，實現與 DaVinci Resolve 的無縫協作。

## 3. GPU 加速管道設計

為了實現對 CUDA、Vulkan、OpenCL、SYCL 等多種 GPU 加速管道的更高效利用，我們需要對 `faster-whisper` 和 `llama-cpp-python` 的配置進行細化。

### 3.1 `faster-whisper` 的 GPU 加速

`faster-whisper` 底層使用 CTranslate2，其 GPU 支援主要通過 CUDA (NVIDIA) 和 cuBLAS (NVIDIA) 實現。對於其他後端：

*   **Metal (Apple Silicon)**：`faster-whisper` 在 macOS 上會自動利用底層的 Core ML 或 Metal Performance Shaders (MPS) 進行加速，無需顯式配置 `device='metal'`。`device='cpu'` 實際上在 M1/M2/M3 上也能獲得 Metal 加速。
*   **Vulkan/OpenCL/SYCL**：`faster-whisper` 本身不直接提供這些後端的顯式選項。若要利用這些，可能需要：
    *   **編譯 CTranslate2 帶有 Vulkan/OpenCL/SYCL 後端**：這是一個複雜的過程，通常需要手動編譯 CTranslate2 庫，並確保 Python 綁定能正確鏈接到這些後端。這超出了插件的範疇，更適合作為 `faster-whisper` 庫本身的優化。
    *   **考慮替代方案**：如果 `faster-whisper` 無法有效利用這些後端，可以考慮 `whisper.cpp` 的 Python 綁定，它對 Vulkan 和 SYCL 有更好的原生支援 [1] [2] [3]。

**建議方案**：

*   **優先使用 `faster-whisper` 的原生支援**：對於 CUDA 和 Metal，保持現有配置。
*   **探索 `whisper.cpp` 整合**：對於 Vulkan/OpenCL/SYCL 用戶，提供選項切換到 `whisper.cpp` 的 Python 綁定，前提是其性能與易用性達到要求。這將需要一個更複雜的模型管理器來處理不同轉錄模型的載入與切換。

### 3.2 `llama-cpp-python` 的 GPU 加速

`llama-cpp-python` 是 `llama.cpp` 的 Python 綁定，對多種 GPU 後端有良好的支援，但通常需要安裝特定編譯版本的 wheel 包 [4] [5]：

*   **CUDA (NVIDIA)**：`pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu`
*   **Vulkan (AMD/Intel Arc)**：`pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/vulkan`
*   **Metal (Apple Silicon)**：`pip install llama-cpp-python` (通常會自動啟用 Metal)
*   **SYCL (Intel GPU)**：`pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/sycl` (需要特定環境配置)

**建議方案**：

*   **增強自動偵測與配置**：`ModelManager` 應能更智能地檢測用戶已安裝的 `llama-cpp-python` 版本及其支援的後端，並據此推薦或自動配置 `n_gpu_layers`。
*   **用戶友好的安裝引導**：在 UI 中提供清晰的指引，告知用戶如何安裝特定 GPU 後端版本的 `llama-cpp-python`，例如提供一鍵複製的 `pip install` 命令。
*   **動態調整 `n_gpu_layers`**：允許用戶在 UI 中調整 `n_gpu_layers`，並提供實時的 VRAM 佔用預估（如果可能），以平衡性能與記憶體使用。

### 3.3 統一的 GPU 後端配置流程

1.  **硬體掃描**：在插件啟動時，執行一次全面的硬體掃描，檢測可用的 GPU 類型 (NVIDIA, AMD, Intel, Apple Silicon)。
2.  **庫版本檢查**：檢查 `faster-whisper` 和 `llama-cpp-python` 的已安裝版本是否支持檢測到的 GPU 後端。
3.  **推薦與配置**：根據掃描結果和庫版本，向用戶推薦最佳的 GPU 配置，並自動設置模型參數。
4.  **手動覆蓋**：允許高級用戶手動選擇或覆蓋 GPU 後端配置。

## 4. 高效 UI 介面設計 (基於 PySide6)

考慮到 DaVinci Resolve Workflow Integration 插件的限制 [6]，以及對豐富 UI 交互的需求，建議採用 **PySide6** 開發一個獨立的應用程式作為插件的用戶介面。這個應用程式可以通過 DaVinci Resolve 的腳本菜單啟動，並通過 Python API 與 DaVinci Resolve 進行通信。

### 4.1 UI 佈局與元素

```mermaid
graph TD
    A[主視窗 (PySide6)] --> B[頂部設定區]
    B --> B1[影片選擇: 瀏覽按鈕 + 路徑顯示]
    B --> B2[Whisper 設定: 模型大小, 語言, 裝置 (自動/CPU/CUDA/Vulkan/SYSL)]
    B --> B3[LLM 設定: 模型路徑, GPU 層數, 上下文大小]
    B --> B4[分析參數: 信心度閾值, 停頓間距, LLM 批次大小]
    B --> B5[暗號設定: 可編輯列表 (新增/刪除/修改)]

    A --> C[操作區]
    C --> C1[開始分析按鈕]
    C2[應用到時間軸按鈕]
    C3[導出 JSON 按鈕]

    A --> D[進度與日誌區]
    D --> D1[進度條 (實時更新)]
    D --> D2[狀態訊息/日誌輸出框]

    A --> E[分析結果預覽區 (可選)]
    E --> E1[表格顯示 Segment 列表 (時間, 文本, 標籤, 建議)]
    E --> E2[可篩選/排序功能]
    E --> E3[選中 Segment 在 DaVinci 中定位 (通過 API)]
```

### 4.2 關鍵 UI 交互設計

1.  **影片選擇**：
    *   提供「瀏覽」按鈕，讓用戶選擇要分析的影片文件。
    *   顯示選定影片的路徑或名稱。
2.  **模型與硬體配置**：
    *   **自動偵測與推薦**：在 UI 啟動時，自動運行硬體偵測，並在 Whisper 和 LLM 設定中預填推薦值。
    *   **下拉選單**：提供 Whisper 模型大小、語言、裝置（自動/CPU/CUDA/Vulkan/SYCL）的下拉選單。
    *   **LLM 模型路徑**：提供「瀏覽」按鈕選擇 GGUF 模型文件，並顯示當前路徑。
    *   **GPU 層數滑塊/輸入框**：允許用戶調整 `LLM_N_GPU_LAYERS`，並提供 VRAM 佔用提示。
    *   **安裝指引**：在 GPU 裝置選擇旁提供一個信息按鈕，點擊後彈出窗口顯示不同 GPU 後端 `llama-cpp-python` 的安裝命令。
3.  **分析參數**：
    *   使用滑塊或數字輸入框調整信心度閾值、停頓間距、LLM 批次大小等。
4.  **暗號設定**：
    *   提供一個可編輯的列表，顯示當前定義的暗號模式。用戶可以新增、編輯或刪除暗號。
    *   每個暗號包含名稱和正則表達式。
5.  **操作按鈕**：
    *   **「開始 AI 分析」**：觸發後端 API 服務進行影片分析。按鈕在分析過程中應禁用，並顯示進度。
    *   **「應用到 DaVinci」**：分析完成後，將結果通過 DaVinci Resolve Scripting API 應用到當前時間軸。
    *   **「導出 JSON」**：將分析結果導出為 JSON 文件。
6.  **進度與日誌**：
    *   **實時進度條**：顯示整體分析進度（音訊提取、轉錄、LLM 分析等）。
    *   **日誌輸出框**：實時顯示後端傳來的日誌信息，包括模型載入、推理時間、發現的問題等，便於用戶排查問題。
7.  **結果預覽 (可選但推薦)**：
    *   以表格形式顯示所有 `Segment` 的詳細信息，包括時間戳、轉錄文本、標籤（flags）、建議等。
    *   提供篩選和排序功能，例如按「可用性」、「標籤」或「信心度」排序。
    *   點擊表格中的某一行，可以通過 DaVinci Resolve API 將時間軸定位到該 Segment 的位置，方便用戶快速審閱。

### 4.3 與 DaVinci Resolve 的整合方式

1.  **啟動方式**：
    *   將 PySide6 應用程式的啟動腳本放置在 DaVinci Resolve 的腳本目錄中 (`/Library/Application Support/Blackmagic Design/DaVinci Resolve/Fusion/Scripts/Utility` 或類似路徑)。
    *   用戶可以從 DaVinci Resolve 的 `Workspace -> Scripts` 選單中啟動插件 UI。
2.  **通信方式**：
    *   PySide6 UI 應用程式將通過 `subprocess` 或其他 IPC (Inter-Process Communication) 機制與後端 API 服務通信。
    *   後端 API 服務在接收到分析請求後，會調用 DaVinci Resolve Scripting API 獲取當前時間軸信息，並在分析完成後將結果寫回。

## 5. 性能優化策略

1.  **異步處理**：將所有耗時的 AI 推理操作放在獨立的線程或進程中執行，確保 UI 響應流暢，避免應用程式卡頓。
2.  **模型緩存**：`ModelManager` 確保模型只載入一次，並在應用程式生命週期內保持在記憶體中，避免重複載入的開銷。
3.  **音訊流處理**：研究 `ffmpeg` 的 Python 綁定，嘗試直接從影片文件流式讀取音訊數據，避免寫入完整的臨時 WAV 文件，減少 I/O 開銷。
4.  **智能批次處理**：LLM 批次處理可以根據可用 GPU 記憶體和模型大小動態調整，以最大化吞吐量，提升處理效率。
5.  **結果增量更新**：對於長影片，可以考慮分段分析並增量更新 UI 和 DaVinci Resolve 時間軸，提供更即時的反饋，改善用戶體驗。

## 6. 優化後的插件代碼 (`davinci_smart_aroll_v2.py`) 使用說明

### 6.1 環境準備

1.  **Python 環境**：確保您的系統安裝了 Python 3.8 或更高版本。
2.  **安裝依賴**：
    *   **PySide6** (UI 框架)：
        ```bash
        pip install PySide6
        ```
    *   **faster-whisper** (語音轉錄)：
        ```bash
        pip install faster-whisper
        ```
    *   **llama-cpp-python** (LLM 推理，**請根據您的硬體選擇安裝**)：
        *   **通用 CPU**：
            ```bash
            pip install llama-cpp-python
            ```
        *   **NVIDIA CUDA**：
            ```bash
            pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu
            ```
        *   **AMD/Intel Arc (Vulkan)**：
            ```bash
            pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/vulkan
            ```
        *   **Apple Silicon (Metal)**：
            ```bash
            CMAKE_ARGS="-DGGML_METAL=on" pip install llama-cpp-python
            # 或直接 pip install llama-cpp-python (通常會自動啟用 Metal)
            ```
        *   **Intel GPU (SYCL)**：
            ```bash
            pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/sycl
            ```
    *   **FFmpeg** (音訊提取工具)：
        *   **macOS**：`brew install ffmpeg`
        *   **Linux**：`sudo apt install ffmpeg`
        *   **Windows**：從 [FFmpeg 官網](https://ffmpeg.org/download.html) 下載並將其路徑添加到系統 PATH 環境變數中。

3.  **LLM 模型**：下載一個 GGUF 格式的 LLM 模型（例如 Qwen2.5-7B-Instruct-Q4_K_M.gguf）並記住其本地路徑。推薦模型可從 HuggingFace 下載：[Qwen/Qwen2.5-7B-Instruct-GGUF](https://huggingface.co/Qwen/Qwen2.5-7B-Instruct-GGUF)。

### 6.2 插件部署

1.  將 `davinci_smart_aroll_v2.py` 文件保存到您的電腦上。
2.  **啟動方式**：
    *   **獨立運行**：直接運行 `python davinci_smart_aroll_v2.py` 即可啟動 UI 應用程式。
    *   **作為 DaVinci Resolve 腳本**：將 `davinci_smart_aroll_v2.py` 放置在 DaVinci Resolve 的腳本目錄中。通常路徑如下：
        *   **macOS**：`/Library/Application Support/Blackmagic Design/DaVinci Resolve/Fusion/Scripts/Utility`
        *   **Windows**：`C:\ProgramData\Blackmagic Design\DaVinci Resolve\Support\Fusion\Scripts\Utility`
        *   **Linux**：`/opt/resolve/Fusion/Scripts/Utility` (或類似路徑)
        然後在 DaVinci Resolve 中，通過 `Workspace -> Scripts` 選單找到並啟動該插件。

### 6.3 插件使用流程

1.  **啟動插件**：按照上述部署方式啟動 `davinci_smart_aroll_v2.py`。
2.  **配置影片路徑**：點擊「瀏覽影片」按鈕，選擇您要分析的影片文件。
3.  **配置 Whisper 模型**：從下拉選單中選擇 `faster-whisper` 的模型大小（例如 `base` 或 `large-v3`）。
4.  **配置 LLM 模型**：點擊「選擇 GGUF」按鈕，選擇您下載的 LLM GGUF 模型文件。調整「GPU 加速層數」以優化性能（`-1` 表示全部層卸載到 GPU）。
5.  **開始分析**：點擊「🚀 開始 AI 分析」按鈕。插件將開始提取音訊、轉錄、進行 LLM 語義分析和規則偵測。進度條和日誌輸出框將實時顯示進度。
6.  **查看結果**：分析完成後，結果將顯示在表格中，包括每個語音片段的狀態、時間、文本和建議。您可以在日誌中查看詳細的分析過程。
7.  **應用到 DaVinci Resolve**：點擊「🎬 應用到 DaVinci」按鈕，插件會將分析結果（Clip 顏色、Marker 等）寫入當前 DaVinci Resolve 時間軸。
8.  **導出結果**：如果需要，可以點擊「導出 JSON」按鈕將分析結果保存為 JSON 文件。

### 6.4 注意事項

*   **首次運行**：首次運行時，`faster-whisper` 模型會自動從 HuggingFace 下載到本地緩存，這可能需要一些時間。
*   **LLM 模型路徑**：請務必確保 LLM 模型路徑正確，否則模型將無法載入。
*   **GPU 記憶體**：如果您的 GPU 記憶體不足，可以嘗試將「GPU 加速層數」設置為 `0`，讓 LLM 在 CPU 上運行。
*   **DaVinci Resolve API**：`davinci_smart_aroll_v2.py` 中的 `apply_to_davinci` 函數需要 DaVinci Resolve Scripting API 的支持。請確保 DaVinci Resolve 正在運行，並且當前項目中打開了時間軸。

## 參考文獻

[1] ggml-org/whisper.cpp. (n.d.). *Help with achieving best performance on Intel CPU + GPU*. Retrieved from https://github.com/ggml-org/whisper.cpp/discussions/2662
[2] Reddit. (n.d.). *Best way to run Whisper through Vulkan?*. Retrieved from https://www.reddit.com/r/LocalLLaMA/comments/1oqm8jh/best_way_to_run_whisper_through_vulkan/
[3] Phoronix. (n.d.). *Whisper.cpp 1.8.3 Delivers A "12x Performance Boost ...*. Retrieved from https://www.phoronix.com/news/Whisper-cpp-1.8.3-12x-Perf
[4] llama-cpp-python. (n.d.). *Getting Started*. Retrieved from https://llama-cpp-python.readthedocs.io/
[5] Reddit. (n.d.). *I've made an "ultimate" guide about building and using `llama.cpp`*. Retrieved from https://www.reddit.com/r/LocalLLaMA/comments/1h2hioi/ive_made_an_ultimate_guide_about_building_and/
[6] Blackmagic Forum. (n.d.). *Workflow Integrations plugin questions*. Retrieved from https://forum.blackmagicdesign.com/viewtopic.php?f=21&t=204982
